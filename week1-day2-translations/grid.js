function drawgrid(num, boxWidth, boxHeight){
    
   nofill();
    Stroke(150);
    for( let j=0; j<rows; j++){
        for(let i = 0; i<cols i++){
    rect(i*20, j*20, 20, 20);// giving 4 parametres
        }
    }
    return "boxes drawn";
}